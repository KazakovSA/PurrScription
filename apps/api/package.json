{
  "name": "@purrscription/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn api.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "pytest tests/ -v",
    "lint": "ruff check api tests",
    "type-check": "echo \"Python type-check via mypy optional\""
  }
}
