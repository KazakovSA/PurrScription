import{useQuery}from'@tanstack/react-query';import{AlertCircle,CheckCircle2,Clock3,FileAudio,KeyRound,ShieldCheck}from'lucide-react';import{Navigate}from'react-router-dom';import{api}from'./api';import{AppShell,PageHeader,roleLabels}from'./AppShell';import{useAppStore}from'./store';import type{Paginated,Project,Task}from'./types';import{EmptyState,ErrorState,Loading}from'./ui';
export function AnalyticsPage(){const projects=useQuery({queryKey:['projects'],queryFn:()=>api<Paginated<Project>>('/projects?limit=100')}),tasks=useQuery({queryKey:['tasks','all'],queryFn:()=>api<Paginated<Task>>('/tasks?limit=100')});if(projects.isLoading||tasks.isLoading)return <AppShell><Loading label="Считаем показатели"/></AppShell>;if(projects.error||tasks.error)return <AppShell><ErrorState error={projects.error||tasks.error}/></AppShell>;const data=tasks.data?.data||[],counts=Object.entries(data.reduce<Record<string,number>>((acc,t)=>{acc[t.status]=(acc[t.status]||0)+1;return acc},{}));return <AppShell><div className="page"><PageHeader eyebrow="Операционная сводка" title="Аналитика" description="Показатели рассчитаны только из доступных вам задач"/><section className="metrics"><div><FileAudio size={18}/><small>Всего задач</small><strong>{data.length}</strong></div><div><Clock3 size={18}/><small>В работе</small><strong>{data.filter(t=>['assigned','in_progress','rework'].includes(t.status)).length}</strong></div><div><ShieldCheck size={18}/><small>На проверке</small><strong>{data.filter(t=>t.status==='review').length}</strong></div><div><CheckCircle2 size={18}/><small>Завершено</small><strong>{data.filter(t=>['accepted','exported'].includes(t.status)).length}</strong></div></section><section className="analytics-grid"><div><h2>Распределение по статусам</h2>{counts.length?counts.map(([status,count])=><div className="bar-row" key={status}><span>{status}</span><i><b style={{width:`${Math.max(5,count/data.length*100)}%`}}/></i><strong>{count}</strong></div>):<EmptyState title="Нет данных" description="Задачи появятся после загрузки медиа."/>}</div><div><h2>Проекты</h2>{projects.data?.data.map(project=><div className="plain-row" key={project.id}><span>{project.name}</span><b>{data.filter(t=>t.projectId===project.id).length}</b></div>)}</div></section></div></AppShell>}
export function TermsPage(){return <AppShell><div className="page"><PageHeader eyebrow="Словарь проекта" title="Термины" description="Единая терминология для аннотаторов и верификаторов"/><EmptyState title="Сервис терминов не подключён" description="Backend v1 не предоставляет endpoint чтения и сохранения терминов. Экран не подменяет сервер локальными или демонстрационными данными." action={<p className="backend-requirement"><AlertCircle size={16}/>Требуется API: GET/POST/PATCH /projects/:id/terms</p>}/></div></AppShell>}
export function AdminPage(){const user=useAppStore(s=>s.user);if(!user)return <Navigate to="/login" replace/>;if(user.role!=='admin')return <Navigate to="/projects" replace/>;return <AppShell><div className="page"><PageHeader eyebrow="Контроль доступа" title="Администрирование" description="Текущая учётная запись и доступные административные операции"/><section className="admin-profile"><div className="avatar large">{user.name.slice(0,1).toUpperCase()}</div><div><h2>{user.name}</h2><p>{user.email}</p><span><KeyRound size={15}/>{roleLabels[user.role]}</span></div></section><EmptyState title="Управление пользователями ожидает API" description="Backend поддерживает регистрацию пользователя администратором, но не предоставляет безопасный список пользователей, редактирование и деактивацию. Поэтому фиктивная таблица пользователей удалена."/></div></AppShell>}
