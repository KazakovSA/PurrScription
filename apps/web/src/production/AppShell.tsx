import type{ReactNode}from'react';import{BarChart3,BookOpen,FileAudio,FolderKanban,LogOut,Settings}from'lucide-react';import{NavLink,useNavigate}from'react-router-dom';import{api}from'./api';import{useAppStore}from'./store';import type{Role}from'./types';
export const roleLabels:Record<Role,string>={admin:'Администратор',supervisor:'Супервайзер',annotator:'Разметчик',verifier:'Верификатор',ml_engineer:'ML-инженер',customer:'Заказчик'};
const items=[['/projects','Проекты',FolderKanban],['/tasks','Задачи',FileAudio],['/terms','Термины',BookOpen],['/analytics','Аналитика',BarChart3],['/admin','Администрирование',Settings]]as const;
export function AppShell({children}:{children:ReactNode}){const user=useAppStore(s=>s.user),clear=useAppStore(s=>s.clearSession),navigate=useNavigate();if(!user)return null;const logout=async()=>{try{await api('/auth/logout',{method:'POST'})}catch{}clear();navigate('/login')};return <div className="app-shell"><aside className="sidebar"><NavLink to="/projects" className="brand"><span className="brand-mark">P</span><span>PurrScription</span></NavLink><nav aria-label="Основная навигация">{items.map(([to,label,Icon])=>{const blocked=to==='/admin'&&user.role!=='admin';return blocked?<span className="nav-disabled" title="Доступно только администратору" key={to}><Icon size={18}/>{label}</span>:<NavLink key={to} to={to}><Icon size={18}/>{label}</NavLink>})}</nav><div className="account"><div className="avatar" aria-hidden>{user.name.slice(0,1).toUpperCase()}</div><div><b>{user.name}</b><small>{roleLabels[user.role]}</small></div><button className="icon-button" aria-label="Выйти" onClick={logout}><LogOut size={17}/></button></div></aside><main className="shell-content">{children}</main></div>}
export function PageHeader({eyebrow,title,description,action}:{eyebrow?:string;title:string;description?:string;action?:ReactNode}){return <header className="page-header"><div>{eyebrow&&<p className="eyebrow">{eyebrow}</p>}<h1>{title}</h1>{description&&<p>{description}</p>}</div>{action}</header>}
