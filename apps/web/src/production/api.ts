import{getAccessToken,useAppStore}from'./store';import type{ApiErrorPayload,AuthSession,Envelope}from'./types';
export const API_BASE=(import.meta.env.VITE_API_URL as string|undefined)?.replace(/\/$/,'')||'/api';
export class ApiError extends Error{constructor(public status:number,public code:string,message:string,public details?:Record<string,unknown>){super(message)}}
export async function api<T>(path:string,options:RequestInit={}):Promise<T>{const headers=new Headers(options.headers),token=getAccessToken();if(token)headers.set('Authorization',`Bearer ${token}`);if(options.body&&!(options.body instanceof FormData))headers.set('Content-Type','application/json');let response:Response;try{response=await fetch(`${API_BASE}${path}`,{...options,headers})}catch{throw new ApiError(0,'NETWORK_ERROR','API недоступен. Запустите backend и повторите запрос.')}if(response.status===204)return undefined as T;const body=await response.json().catch(()=>null)as ApiErrorPayload|T|null;if(!response.ok){const p=body as ApiErrorPayload|null;if(response.status===401)useAppStore.getState().clearSession();throw new ApiError(response.status,p?.error?.code||'REQUEST_FAILED',p?.error?.message||`HTTP ${response.status}`,p?.error?.details)}return body as T}
export async function loginRequest(email:string,password:string){return(await api<Envelope<AuthSession>>('/auth/login',{method:'POST',body:JSON.stringify({email,password})})).data}
export const mediaUrl=(id:string)=>`${API_BASE}/media/${id}`;
export function mediaFetchParams():RequestInit{const token=getAccessToken();return token?{headers:{Authorization:`Bearer ${token}`}}:{}}
